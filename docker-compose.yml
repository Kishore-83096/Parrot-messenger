services:
  messenger:
    build: .
    container_name: parrot-messenger
    env_file:
      - path: .env
        format: raw
    environment:
      PORT: 8000
      ALLOWED_HOSTS: localhost,127.0.0.1,0.0.0.0
    ports:
      - "8000:8000"
    restart: unless-stopped
